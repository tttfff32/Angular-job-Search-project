import { MyJobDirectiveDirective } from './my-job-directive.directive';

describe('MyJobDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new MyJobDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
